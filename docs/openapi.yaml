openapi: 3.0.3
info:
  title: My API
  version: 1.0.0
servers:
  - url: /api
paths:
  /ping:
    get:
      summary: Ping
      responses:
        "200":
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  status:
                    type: string
              example:
                status: ok
